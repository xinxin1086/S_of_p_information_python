# 数据流图说明文档

## 项目概述

这是一个基于 Flask 框架的后端服务系统，采用模块化设计，提供用户管理、科普文章、论坛、活动、公告等核心功能。

---

## 一、顶层数据流图 (Context Diagram - Level 0 DFD)

### 1.1 顶层图说明

顶层图展示系统与外部实体之间的交互，不涉及系统内部的处理细节。

### 1.2 顶层图

```
                    ┌─────────────────────────────────────────────┐
                    │                                             │
                    │         Flask 后端服务系统                  │
                    │    (S_of_p_information_python)             │
                    │                                             │
                    │  ┌───────────────────────────────────────┐ │
                    │  │        API 网关层                     │ │
                    │  │  - 认证授权 (JWT)                     │ │
                    │  │  - CORS 跨域处理                      │ │
                    │  │  - 路由分发                           │ │
                    │  └───────────────────────────────────────┘ │
                    │                                             │
                    └─────────────────────────────────────────────┘
                                      │
                ┌─────────────────────┼─────────────────────┐
                │                     │                     │
                ▼                     ▼                     ▼
        ┌───────────────┐    ┌───────────────┐    ┌───────────────┐
        │               │    │               │    │               │
        │  前端应用     │    │  MySQL 数据库  │    │  文件存储系统  │
        │               │    │               │    │               │
        │ - Vue3 前端   │    │ - 用户数据    │    │ - 图片存储    │
        │ - 管理端界面  │    │ - 内容数据    │    │ - 附件存储    │
        │ - 用户端界面  │    │ - 互动数据    │    │ - 头像存储    │
        │               │    │ - 系统配置    │    │               │
        └───────────────┘    └───────────────┘    └───────────────┘
```

### 1.3 外部实体说明

| 外部实体 | 说明 | 与系统交互 |
|---------|------|-----------|
| **前端应用** | Vue3 构建的用户端和管理端界面 | 发送 HTTP 请求，接收 JSON 响应 |
| **MySQL 数据库** | 存储所有业务数据的数据库 | 读写数据（通过 SQLAlchemy ORM） |
| **文件存储系统** | 本地文件系统，存储图片和附件 | 读写文件（图片、附件等） |

### 1.4 数据流向说明

#### 前端应用 ↔ 后端系统
```
请求数据流：
  HTTP 请求 → JSON 格式数据 → JWT Token → 业务参数

响应数据流：
  JSON 响应 ← 统一响应格式 ← 业务数据/错误信息
```

#### 后端系统 ↔ MySQL 数据库
```
写入数据流：
  ORM 操作 → INSERT/UPDATE/DELETE SQL → 数据写入

读取数据流：
  查询结果 ← SELECT SQL ← 数据检索
```

#### 后端系统 ↔ 文件存储系统
```
文件写入流：
  验证文件 → 保存文件 → 返回文件路径

文件读取流：
  文件内容 ← 文件路径 ← 静态文件服务
```

---

## 二、0 层数据流图 (Level 1 DFD)

### 2.1 0 层图说明

0 层图展示系统内部的主要处理过程和数据存储，揭示系统的功能模块划分。

### 2.2 0 层图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           前端应用 (外部实体)                               │
└─────────────────────────────────────────────────────────────────────────────┘
                                  │
                    ┌─────────────┼─────────────┐
                    │             │             │
                    ▼             ▼             ▼
           ┌────────────┐ ┌────────────┐ ┌────────────┐
           │  公开访问  │ │  用户操作  │ │ 管理员操作 │
           │    请求    │ │    请求    │ │    请求    │
           └────────────┘ └────────────┘ └────────────┘
                    │             │             │
                    └─────────────┼─────────────┘
                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                        Flask 后端服务系统                                   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                     1.0 认证授权处理                                │   │
│  │  - JWT Token 验证                                                    │   │
│  │  - 用户身份识别                                                      │   │
│  │  - 权限检查                                                          │   │
│  │  输入: HTTP 请求 + Token (可选)                                      │   │
│  │  输出: 已认证用户信息 / 认证失败                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                     2.0 路由分发处理                                │   │
│  │  - Blueprint 路由匹配                                               │   │
│  │  - 请求参数解析                                                      │   │
│  │  - 请求验证                                                          │   │
│  │  输入: HTTP 请求 + 路由路径                                          │   │
│  │  输出: 分发到各业务模块                                              │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│  ┌─────────────────┬─────────────────┬─────────────────┬───────────────┐   │
│  │                 │                 │                 │               │   │
│  │  3.1 用户管理   │  3.2 科普管理   │  3.3 论坛管理   │  3.4 活动管理 │   │
│  │                 │                 │                 │               │   │
│  │ - 注册登录      │ - 文章发布      │ - 帖子创建      │ - 活动创建    │   │
│  │ - 信息管理      │ - 文章浏览      │ - 楼层回复      │ - 活动预约    │   │
│  │ - 密码管理      │ - 点赞收藏      │ - 点赞关注      │ - 评分讨论    │   │
│  │ - 用户管理      │ - 分类管理      │ - 内容审核      │ - 讨论管理    │   │
│  └─────────────────┴─────────────────┴─────────────────┴───────────────┘   │
│                                    │                                        │
│  ┌─────────────────┬─────────────────┬─────────────────┬───────────────┐   │
│  │                 │                 │                 │               │   │
│  │  3.5 公告管理   │  3.6 公共服务   │  3.7 文件处理   │  3.8 统计分析 │   │
│  │                 │                 │                 │               │   │
│  │ - 公告发布      │ - 附件管理      │ - 图片上传      │ - 用户统计    │   │
│  │ - 公告浏览      │ - 兼容接口      │ - 图片删除      │ - 内容统计    │   │
│  │ - 已读记录      │ - 健康检查      │ - 头像上传      │ - 活动统计    │   │
│  │ - 分类管理      │                 │                 │               │   │
│  └─────────────────┴─────────────────┴─────────────────┴───────────────┘   │
│                                    │                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                     4.0 数据处理层                                  │   │
│  │  - SQLAlchemy ORM 操作                                              │   │
│  │  - 事务管理                                                          │   │
│  │  - 数据验证                                                          │   │
│  │  - 数据转换                                                          │   │
│  │  输入: 业务逻辑请求                                                  │   │
│  │  输出: 数据库操作结果                                                │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                     5.0 响应格式化                                  │   │
│  │  - 统一响应格式 (ResponseService)                                   │   │
│  │  - 成功响应封装                                                      │   │
│  │  - 错误响应封装                                                      │   │
│  │  - 分页数据封装                                                      │   │
│  │  输入: 业务处理结果                                                  │   │
│  │  输出: JSON 响应                                                    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                  │
                    ┌─────────────┼─────────────┐
                    │             │             │
                    ▼             ▼             ▼
         ┌────────────────┐ ┌────────────┐ ┌────────────┐
         │                │ │            │ │            │
         │ D1: MySQL 数据库│ │ D2: 文件   │ │ D3: 临时   │
         │                │ │   存储     │ │   数据     │
         │ - 用户数据     │ │            │ │            │
         │ - 内容数据     │ │ - 图片     │ │ - Session  │
         │ - 互动数据     │ │ - 附件     │ │ - Cache    │
         │ - 系统配置     │ │ - 头像     │ │            │
         └────────────────┘ └────────────┘ └────────────┘
```

### 2.3 处理过程说明

#### 1.0 认证授权处理
**功能**：验证用户身份和权限

**输入数据流**：
- HTTP 请求
- JWT Token（可选，登录后必需）
- 请求头信息

**处理逻辑**：
1. 检查请求是否需要认证
2. 提取并解析 JWT Token
3. 验证 Token 有效性和过期时间
4. 获取用户信息
5. 检查用户权限（基于角色）

**输出数据流**：
- 已认证用户信息 → 路由分发处理
- 认证失败响应 → 前端应用

**相关模块**：
- `components/token_required.py`
- `components/permissions.py`

---

#### 2.0 路由分发处理
**功能**：将请求分发到对应的业务模块

**输入数据流**：
- HTTP 请求（来自认证处理）
- 路由路径
- 请求参数（Query/Body/JSON）

**处理逻辑**：
1. 匹配 Blueprint 路由
2. 解析请求参数
3. 参数验证
4. 调用对应的业务处理函数

**输出数据流**：
- 业务请求 → 各业务模块

**相关模块**：
- `app.py`（Blueprint 注册）
- 各 API 模块的路由定义

---

#### 3.1 用户管理模块
**功能**：处理用户相关的所有操作

**输入数据流**：
- 用户注册请求
- 用户登录请求
- 用户信息更新请求
- 密码修改请求
- 管理员用户管理请求

**处理逻辑**：
1. **注册流程**：
   - 验证账号唯一性
   - 密码加密存储
   - 创建用户记录
   - 生成 JWT Token

2. **登录流程**：
   - 验证账号密码
   - 检查用户状态
   - 生成 JWT Token

3. **信息管理**：
   - 查询用户信息
   - 更新用户资料
   - 软删除用户（匿名化）

**输出数据流**：
- 用户数据/Token → 响应格式化
- 用户信息 → D1: MySQL 数据库
- 头像文件 → D2: 文件存储

**主要 API 端点**：
```
POST /api/user/auth/register
POST /api/user/auth/login
GET  /api/user/user/info
POST /api/user/user/update
POST /api/user/auth/change-password
GET  /api/admin/users
PUT  /api/admin/users/<id>
DELETE /api/admin/users/<id>
```

**相关模块**：
- `API_user/auth_bp.py`（认证）
- `API_user/user_bp.py`（用户信息）
- `API_user/admin_bp.py`（管理员）

---

#### 3.2 科普管理模块
**功能**：管理科普文章相关内容

**输入数据流**：
- 文章列表查询请求
- 文章详情查询请求
- 文章创建/更新/删除请求
- 点赞/浏览操作请求
- 分类管理请求

**处理逻辑**：
1. **公开访问**：
   - 查询已发布文章
   - 分页展示
   - 按分类筛选

2. **用户操作**：
   - 记录浏览次数
   - 处理点赞
   - 防止重复操作

3. **管理员操作**：
   - 创建/编辑文章
   - 发布/撤回文章
   - 管理文章分类

**输出数据流**：
- 文章数据 → 响应格式化
- 文章记录 → D1: MySQL 数据库
- 浏览/点赞记录 → D1: MySQL 数据库
- 文章封面图片 → D2: 文件存储

**主要 API 端点**：
```
GET  /api/public/science/articles
GET  /api/public/science/articles/<id>
POST /api/science/articles/<id>/like
GET  /api/science/articles/<id>/visit
POST /api/science/admin/articles
PUT  /api/science/admin/articles/<id>
DELETE /api/science/admin/articles/<id>
GET  /api/science/categories
POST /api/science/categories
```

**相关模块**：
- `API_science/bp_science_public.py`
- `API_science/bp_science_user.py`
- `API_science/bp_science_admin.py`
- `API_science/bp_science_category.py`

---

#### 3.3 论坛管理模块
**功能**：管理论坛帖子、楼层、回复

**输入数据流**：
- 帖子列表/详情查询请求
- 帖子创建/编辑/删除请求
- 楼层回复请求
- 二级回复请求
- 点赞/关注请求

**处理逻辑**：
1. **帖子管理**：
   - 创建主题帖
   - 编辑/删除帖子
   - 按版块分类

2. **楼层管理**：
   - 一级回复（楼层）
   - 二级回复（对楼层的回复）
   - 楼层排序

3. **互动功能**：
   - 点赞帖子/楼层/回复
   - 浏览记录
   - 用户关注

**输出数据流**：
- 帖子/回复数据 → 响应格式化
- 帖子/楼层/回复 → D1: MySQL 数据库
- 互动记录 → D1: MySQL 数据库

**主要 API 端点**：
```
GET  /api/forum/posts
POST /api/forum/posts
PUT  /api/forum/posts/<id>
DELETE /api/forum/posts/<id>
POST /api/forum/posts/<id>/floors
GET  /api/forum/floors/<id>
POST /api/forum/floors/<id>/replies
GET  /api/forum/replies/<id>
```

**相关模块**：
- `API_forum/post_bp.py`
- `API_forum/floor_bp.py`
- `API_forum/reply_bp.py`

---

#### 3.4 活动管理模块
**功能**：管理活动、预约、评分、讨论

**输入数据流**：
- 活动列表/详情查询请求
- 活动创建/更新请求
- 预约请求
- 评分请求
- 讨论留言请求

**处理逻辑**：
1. **活动管理**：
   - 创建活动
   - 编辑活动信息
   - 发布/取消活动

2. **预约管理**：
   - 用户预约活动
   - 查看预约状态
   - 取消预约

3. **评分与讨论**：
   - 活动评分
   - 发起讨论
   - 讨论留言

**输出数据流**：
- 活动数据 → 响应格式化
- 活动记录 → D1: MySQL 数据库
- 预约记录 → D1: MySQL 数据库
- 评分记录 → D1: MySQL 数据库
- 讨论内容 → D1: MySQL 数据库

**主要 API 端点**：
```
GET  /api/public/activities/activities
POST /api/activities
GET  /api/activities/admin/activities
PUT  /api/activities/admin/activities/<id>
POST /api/activities/booking/activities/<id>/book
GET  /api/activities/booking/my-bookings
POST /api/activities/user/activities/<id>/rating
POST /api/activities/discussion/activities/<id>/discussions
GET  /api/activities/discussion/discussions/<id>
```

**相关模块**：
- `API_activities/root_bp.py`
- `API_activities/user_ops_bp.py`
- `API_activities/admin_manage_bp.py`
- `API_activities/booking_bp.py`
- `API_activities/discussion_bp.py`

---

#### 3.5 公告管理模块
**功能**：管理系统公告和通知

**输入数据流**：
- 公告列表/详情查询请求
- 公告发布/更新/删除请求
- 已读标记请求
- 分类查询请求

**处理逻辑**：
1. **公告管理**：
   - 管理员发布公告
   - 编辑/删除公告
   - 按分类管理

2. **已读追踪**：
   - 记录用户已读
   - 统计未读数量
   - 已读列表查询

**输出数据流**：
- 公告数据 → 响应格式化
- 公告记录 → D1: MySQL 数据库
- 已读记录 → D1: MySQL 数据库
- 公告附件 → D2: 文件存储

**主要 API 端点**：
```
GET  /api/public/notice
GET  /api/public/notice/<id>
GET  /api/notice/user/unread
GET  /api/notice/user/notices
POST /api/notice/user/<id>/read
POST /api/notice/admin/notices
PUT  /api/notice/admin/notices/<id>
DELETE /api/notice/admin/notices/<id>
GET  /api/notice/categories
```

**相关模块**：
- `API_notice/bp_notice_public.py`
- `API_notice/bp_notice_user.py`
- `API_notice/bp_notice_admin.py`
- `API_notice/bp_notice_category.py`

---

#### 3.6 公共服务模块
**功能**：提供公共接口服务

**输入数据流**：
- 附件上传请求
- 附件列表/删除请求
- 用户信息查询请求
- 兼容接口请求

**处理逻辑**：
1. **附件管理**：
   - 上传附件
   - 查询附件列表
   - 删除附件

2. **用户信息**：
   - 公开用户信息查询
   - 用户头像获取

**输出数据流**：
- 附件信息 → 响应格式化
- 附件文件 → D2: 文件存储
- 用户信息 → 响应格式化

**主要 API 端点**：
```
POST /api/common/attachment
GET  /api/common/attachments
DELETE /api/common/attachment/<id>
GET  /api/common/user/info
```

**相关模块**：
- `common/common_bp.py`

---

#### 3.7 文件处理模块
**功能**：处理图片和文件上传

**输入数据流**：
- 图片上传请求
- 图片删除请求
- 头像上传请求

**处理逻辑**：
1. **文件验证**：
   - 文件类型检查
   - 文件大小限制（最大 5MB）
   - 文件名安全处理

2. **存储处理**：
   - 生成唯一文件名
   - 保存到指定目录
   - 返回访问路径

**输出数据流**：
- 文件路径 → 响应格式化
- 文件内容 → D2: 文件存储

**主要 API 端点**：
```
POST /api/common/upload/image
POST /api/common/delete/image
POST /api/common/upload/avatar
```

**相关模块**：
- `common/common_bp.py`
- `components/image_storage.py`

---

#### 3.8 统计分析模块
**功能**：提供数据统计和分析

**输入数据流**：
- 用户统计请求
- 内容统计请求
- 活动统计请求

**处理逻辑**：
1. **数据聚合**：
   - 统计用户数量
   - 统计内容数量
   - 统计互动数据

2. **数据分析**：
   - 趋势分析
   - 活跃度分析
   - 报表生成

**输出数据流**：
- 统计数据 → 响应格式化

**主要 API 端点**：
```
GET /api/admin/statistics/users
GET /api/admin/statistics/activities
```

**相关模块**：
- `API_admin/bp_admin_stats.py`

---

#### 4.0 数据处理层
**功能**：统一处理所有数据库操作

**输入数据流**：
- 数据库查询请求
- 数据写入请求
- 数据更新/删除请求

**处理逻辑**：
1. **ORM 操作**：
   - SQLAlchemy 模型查询
   - 关联数据加载
   - 事务管理

2. **数据验证**：
   - 字段验证
   - 外键约束检查
   - 唯一性检查

**输出数据流**：
- 查询结果 → 各业务模块
- 操作结果确认 → 各业务模块

**相关模块**：
- `components/db.py`
- `components/models.py`

---

#### 5.0 响应格式化
**功能**：统一处理所有 API 响应

**输入数据流**：
- 业务处理成功结果
- 业务处理失败错误
- 分页数据

**处理逻辑**：
1. **成功响应**：
   ```json
   {
     "success": true,
     "data": {...},
     "message": "操作成功"
   }
   ```

2. **错误响应**：
   ```json
   {
     "success": false,
     "error": "错误信息",
     "code": "ERROR_CODE"
   }
   ```

3. **分页响应**：
   ```json
   {
     "success": true,
     "data": [...],
     "pagination": {
       "page": 1,
       "page_size": 10,
       "total": 100
     }
   }
   ```

**输出数据流**：
- JSON 响应 → 前端应用

**相关模块**：
- `components/response_service.py`

---

### 2.4 数据存储说明

#### D1: MySQL 数据库
**存储内容**：
- 用户数据（user_info, admin_info）
- 科普文章（science_articles）
- 论坛内容（forum_posts, forum_floors, forum_replies）
- 活动数据（activities）
- 公告数据（notices）
- 互动数据（点赞、浏览、评分）
- 系统配置

**主要数据表**：
```
用户相关：
  - user_info          普通用户表
  - admin_info         管理员表
  - deleted_user       已注销用户表

内容相关：
  - science_articles   科普文章表
  - forum_posts        论坛帖子表
  - forum_floors       论坛楼层表
  - forum_replies      论坛回复表
  - activities         活动表
  - notices            公告表

互动相关：
  - science_article_likes    文章点赞表
  - science_article_visits   文章浏览表
  - forum_likes              论坛点赞表
  - activity_bookings        活动预约表
  - activity_ratings         活动评分表
  - notice_read_records      公告已读表
```

---

#### D2: 文件存储
**存储内容**：
- 用户头像
- 文章封面图片
- 活动图片
- 公告附件
- 其他图片资源

**存储路径**：
```
static/images/
  ├── avatars/          用户头像
  ├── articles/         文章封面
  ├── activities/       活动图片
  ├── notices/          公告附件
  └── forum/            论坛图片
```

---

#### D3: 临时数据
**存储内容**：
- Session 数据
- 缓存数据
- 临时文件

---

## 三、关键数据流示例

### 3.1 用户登录数据流

```
前端应用
  │
  │  POST /api/user/auth/login
  │  { account: "user123", password: "******" }
  ▼
认证授权处理
  │
  │  验证请求参数
  ▼
路由分发处理
  │
  │  分发到用户管理模块
  ▼
3.1 用户管理模块
  │
  │  查询用户信息
  ▼
4.0 数据处理层
  │
  │  SELECT * FROM user_info WHERE account = 'user123'
  ▼
D1: MySQL 数据库
  │
  │  返回用户记录
  ▼
4.0 数据处理层
  │
  │  验证密码
  │  生成 JWT Token
  ▼
5.0 响应格式化
  │
  │  { success: true, data: { token: "...", user: {...} } }
  ▼
前端应用
```

---

### 3.2 发布科普文章数据流

```
前端应用
  │
  │  POST /api/science/admin/articles
  │  Headers: Authorization: Bearer <token>
  │  Body: { title: "...", content: "...", category_id: 1 }
  ▼
1.0 认证授权处理
  │
  │  验证 JWT Token
  │  检查管理员权限
  ▼
2.0 路由分发处理
  │
  │  分发到科普管理模块
  ▼
3.2 科普管理模块
  │
  │  创建文章记录
  ▼
4.0 数据处理层
  │
  │  INSERT INTO science_articles (...)
  ▼
D1: MySQL 数据库
  │
  │  返回新建文章 ID
  ▼
3.2 科普管理模块
  │
  │  处理封面图片（如有）
  ▼
3.7 文件处理模块
  │
  │  保存图片到 static/images/articles/
  ▼
D2: 文件存储
  │
  │  返回图片路径
  ▼
5.0 响应格式化
  │
  │  { success: true, data: { id: 123, ... } }
  ▼
前端应用
```

---

### 3.3 活动预约数据流

```
前端应用
  │
  │  POST /api/activities/booking/activities/5/book
  │  Headers: Authorization: Bearer <token>
  ▼
1.0 认证授权处理
  │
  │  验证用户身份
  ▼
2.0 路由分发处理
  │
  │  分发到活动管理模块
  ▼
3.4 活动管理模块
  │
  │  检查活动是否可预约
  │  检查用户是否已预约
  ▼
4.0 数据处理层
  │
  │  SELECT * FROM activities WHERE id = 5
  │  SELECT * FROM activity_bookings WHERE activity_id = 5 AND user_id = ...
  ▼
D1: MySQL 数据库
  │
  │  返回查询结果
  ▼
3.4 活动管理模块
  │
  │  创建预约记录
  ▼
4.0 数据处理层
  │
  │  INSERT INTO activity_bookings (...)
  ▼
D1: MySQL 数据库
  │
  │  返回预约记录 ID
  ▼
5.0 响应格式化
  │
  │  { success: true, data: { booking_id: 789, ... } }
  ▼
前端应用
```

---

## 四、数据流图总结

### 4.1 系统特点

1. **清晰的分层架构**：
   - 顶层：外部实体交互
   - 中间层：业务处理
   - 底层：数据存储

2. **统一的认证授权**：
   - 所有需要认证的请求都通过 1.0 处理
   - 支持基于角色的权限控制

3. **模块化设计**：
   - 每个业务模块独立处理
   - 通过 Blueprint 实现功能解耦

4. **统一的响应格式**：
   - 所有响应通过 5.0 统一格式化
   - 便于前端处理

### 4.2 数据流向总结

```
外部请求
  ↓
认证授权（1.0）
  ↓
路由分发（2.0）
  ↓
业务处理（3.1-3.8）
  ↓
数据处理（4.0）
  ↓
数据存储（D1/D2）
  ↓
业务处理
  ↓
响应格式化（5.0）
  ↓
外部响应
```

### 4.3 主要数据流

| 数据流 | 起点 → 终点 | 数据内容 |
|--------|-----------|---------|
| 用户请求 | 前端 → 认证处理 | HTTP 请求 + Token |
| 业务请求 | 路由分发 → 业务模块 | 业务参数 |
| 数据查询 | 业务模块 → 数据处理层 | 查询条件 |
| 查询结果 | 数据库 → 业务模块 | 数据记录 |
| 业务响应 | 业务模块 → 响应格式化 | 业务数据 |
| JSON 响应 | 响应格式化 → 前端 | JSON 数据 |

---

## 附录：技术栈说明

| 技术 | 版本 | 用途 |
|------|------|------|
| Flask | 3.1.2 | Web 框架 |
| SQLAlchemy | 2.0.45 | ORM 框架 |
| PyJWT | 2.10.1 | JWT 认证 |
| Flask-CORS | - | 跨域处理 |
| MySQL | - | 关系型数据库 |
| Pytest | 9.0.2 | 测试框架 |

---

**文档版本**: v1.0
**生成日期**: 2025-03-02
**项目名称**: S_of_p_information_python
